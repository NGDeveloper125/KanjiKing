@inject IJSRuntime JSRuntime

<audio id="@AudioId"></audio>

@code {
    private string AudioId = Guid.NewGuid().ToString();

    public async Task Play(string audioFileName)
    {
        await JSRuntime.InvokeVoidAsync("playAudio", AudioId, $"Audio/{audioFileName}");
    }
}